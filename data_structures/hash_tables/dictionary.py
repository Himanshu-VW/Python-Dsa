#using 
from collections import defaultdict

default = defaultdict(int)
'''it initializes any element not present as key with 0 so we don't get a key error
without actually initializing manually we can just do out operation on it like append or in case of int + or -'''
